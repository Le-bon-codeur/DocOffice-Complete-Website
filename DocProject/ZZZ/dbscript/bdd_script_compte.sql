create database doctor_office_useracc;

CREATE TABLE `247_78` (
    `_id` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `_name` VARCHAR(45) NOT NULL,
    `_email` VARCHAR(45) NOT NULL,
    `_hash` VARCHAR(60) NOT NULL
) ENGINE = INNODB;

-- this database is no longer used --